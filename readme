poke接口自动化框架 python+pytest+requests+allure
config:配置封装
    config.yaml:配置文件
    reconfig.py:读取配置文件
commom:工具类封装
    GetYaml:读取数据
    RequestPost:封装post方法
    RequestGet:封装get方法
    Logs:封装日志读取类
conftest.py:初始化文件
pytest.ini:配置文件
report:allure图形化报告文件位置（不需要上传git）
tests:测试case
    conftest.py:  用于根据主目录中的conftest读取环境配置，通过配置读取对应的测试数据，用户参数化
datas: 测试数据
    debug: 测试环境
    online: 线上环境

Todo(sunze)
1.完成日志相关配置  0.7
2.完成allure配置   0.7
3.搭建接口依赖的case,完成获取token的公共接口
4.接入jsonpath进行assert
5.后续接入email jekins
6.对assret进行封装

